{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Label and group Python ecosystem updates, keeping majors in their own PR",
  "packageRules": [
    {
      "description": "Label all Python updates",
      "matchCategories": [
        "python"
      ],
      "addLabels": [
        "python"
      ]
    },
    {
      "description": "Group non-major Python updates into a single PR",
      "matchCategories": [
        "python"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin"
      ],
      "groupName": "Python updates"
    },
    {
      "description": "Label all Copier template updates. copier is already matched by the python category rules above, so this adds the copier label on top rather than replacing them",
      "matchManagers": [
        "copier"
      ],
      "addLabels": [
        "python",
        "copier"
      ]
    },
    {
      "description": "Group non-major Copier template updates into a single PR. copier is itself in the python category, so this rule must stay after the category rules above in order to override their group name",
      "matchManagers": [
        "copier"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin"
      ],
      "groupName": "Copier updates"
    }
  ]
}
