{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Enable the pre-commit manager, which is off by default, then label and group its updates. Note this manager reads .pre-commit-config.yaml only, not prek.toml",
  "pre-commit": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Label all pre-commit updates",
      "matchManagers": [
        "pre-commit"
      ],
      "addLabels": [
        "pre-commit"
      ]
    },
    {
      "description": "Group non-major pre-commit updates into a single PR",
      "matchManagers": [
        "pre-commit"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin"
      ],
      "groupName": "Pre-commit updates"
    }
  ]
}
