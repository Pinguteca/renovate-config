{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Label every container image wherever it is declared, and group the ones declared in container-native files. Images inside Kubernetes manifests keep this label but are regrouped by kubernetes-config",
  "packageRules": [
    {
      "description": "Label every container image, whatever manager declares it",
      "matchDatasources": [
        "docker"
      ],
      "addLabels": [
        "container"
      ]
    },
    {
      "description": "Group non-major images declared in container-native files into a single PR",
      "matchManagers": [
        "dockerfile",
        "docker-compose",
        "quadlet",
        "devcontainer",
        "buildpacks"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin"
      ],
      "groupName": "Container image updates"
    }
  ]
}
