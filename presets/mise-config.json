{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Label and group mise tool updates, keeping majors in their own PR. Uses the native mise manager, which resolves registry short names and backend prefixes such as aqua: and also maintains mise.lock",
  "packageRules": [
    {
      "description": "Label all mise tool updates",
      "matchManagers": [
        "mise"
      ],
      "addLabels": [
        "mise"
      ]
    },
    {
      "description": "Group non-major mise tool updates into a single PR",
      "matchManagers": [
        "mise"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin"
      ],
      "groupName": "Mise updates"
    }
  ]
}
